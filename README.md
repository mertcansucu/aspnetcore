### git commands:

```console
    git status -> değişiklikleri gösterir
    git add . -> tüm dosyaları gite ekler
    git commit -m "commit message" -> commit oluşturur
    git log -> commitleri gösterir
    git push -u origin head -> origine mevcut branchine gönderir (github main banchine)
```